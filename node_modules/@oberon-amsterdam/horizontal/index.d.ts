/// <reference types="node" />
import { EventEmitter } from 'events';
export interface Options {
    scrollAmount?: number;
    scrollAmountStep?: number;
    container?: HTMLElement;
    showScrollbars?: boolean;
    preventVerticalScroll?: boolean;
}
declare global {
    interface CSSStyleDeclaration {
        overscrollBehaviorX: string;
        overscrollBehaviorY: string;
    }
}
export default class HorizontalScroll extends EventEmitter {
    private springSystem;
    private spring;
    private container;
    private observer;
    private containerIsIntersecting;
    private style;
    private cssClass;
    private preventVerticalScroll;
    private blacklist;
    /**
     * Initialize a new horizontal scroll instance.
     * Will immediately bind to container.
     *
     */
    constructor({ scrollAmount, scrollAmountStep, container, showScrollbars, preventVerticalScroll, }?: Options);
    destroy(): void;
    private wheel;
    private keydown;
}
